import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class ControlsHelp here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class ControlsHelp extends Menus
{
    boolean onTop;
    public ControlsHelp() {
        onTop = false;
    }
    public void act()
    {
        //if (!onTop && this != null) {
        //    getWorld().setPaintOrder(MenuArrow.class, ControlsHelp.class, MenuBackground.class);
        //    onTop = true;
        //}
        //if (!onTop) {
        //    getWorld().setPaintOrder(MenuArrow.class, ControlsHelp.class, MenuBackground.class);
        //    onTop = true;
        //}
    }
}
